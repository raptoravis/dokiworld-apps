{
  "name": "dokiworld-game-match3",
  "version": "1.0.7",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "generate:manifest": "node scripts/generate-manifest.mjs",
    "build": "node scripts/build.mjs"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  },
  "dependencies": {
    "@dokiworld/app-sdk": "^3.1.0"
  }
}
